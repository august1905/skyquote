import AppShell from '../components/AppShell';

// Placeholder — the real list view (folders + search, no tabs) is a later phase.
function Templates() {
	return (
		<AppShell>
			<h1>Templates</h1>
			<p>Template list coming soon.</p>
		</AppShell>
	);
}

export default Templates;
