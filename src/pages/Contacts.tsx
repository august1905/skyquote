import AppShell from '../components/AppShell';

// Placeholder — the real list view (contacts synced from Zoho CRM) is a
// later phase, and depends on the CRM import mechanism being decided.
function Contacts() {
	return (
		<AppShell>
			<h1>Contacts</h1>
			<p>Contact list coming soon.</p>
		</AppShell>
	);
}

export default Contacts;
